SELECT r.FOOD_TYPE, r.REST_ID, r.REST_NAME, r.FAVORITES
FROM REST_INFO r
JOIN (
    SELECT FOOD_TYPE, MAX(FAVORITES) AS fav
    FROM REST_INFO
    GROUP BY FOOD_TYPE
) AS m
ON r.FOOD_TYPE = m.FOOD_TYPE
AND r.FAVORITES = m.fav
ORDER BY r.FOOD_TYPE DESC;
